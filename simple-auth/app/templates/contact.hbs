<div class="container-fluid ">
    <div class="row">
        <div class="col-md-6">
            {{#each model as |teamUnit|}}
                {{team-listing teammember=teamUnit}}
            {{/each}}
        </div>
    </div>
</div>
